---
phase: 01-python-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: true
requirements:
  - DEP-01
  - DEP-02
must_haves:
  truths:
    - "requirements.txt содержит ragas, langchain-openai, langchain-core"
    - "deepeval==3.9.3 остаётся закреплён и не удалён"
    - "pip install -r requirements.txt проходит без конфликтов версий"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependency list"
      contains: "ragas"
  key_links:
    - from: "requirements.txt"
      to: "eval/eval_ragas_metrics.py"
      via: "import ragas, langchain_openai"
      pattern: "ragas>=0\\.2"
---

<objective>
Добавить зависимости RAGAS (ragas, langchain-openai, langchain-core) в requirements.txt, сохранив существующий deepeval==3.9.3 и остальные закреплённые версии без изменений.

Purpose: Плана 03 (pipeline) не сможет импортировать `ragas`, `LangchainLLMWrapper`, `ChatOpenAI`, пока эти пакеты не добавлены. Согласно D-01 и PITFALLS.md #1, нужно ставить именно `langchain-openai` и `langchain-core`, а НЕ монолитный `langchain`.
Output: Обновлённый requirements.txt с тремя новыми строками.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add RAGAS + langchain dependencies to requirements.txt</name>
  <read_first>
    - requirements.txt (весь файл — 17 строк)
    - .planning/research/STACK.md (раздел "Зависимости ragas")
    - .planning/research/PITFALLS.md (пункт 1 "Конфликт зависимостей langchain")
  </read_first>
  <files>requirements.txt</files>
  <action>
    Дописать В КОНЕЦ файла requirements.txt (после строки `httpx>=0.28.0`) ровно три строки, в указанном порядке:

    ```
    ragas>=0.2.0
    langchain-openai>=0.1.0
    langchain-core>=0.2.0
    ```

    Правила (per DEP-01, DEP-02, PITFALLS.md #1):
    - НЕ удалять и не менять существующие 17 строк
    - НЕ добавлять пакет `langchain` (монолит) — ТОЛЬКО `langchain-openai` и `langchain-core`, иначе конфликт с новым разделённым API
    - Версии задавать через `>=` (range), чтобы pip resolver мог ужить их с существующим `openai==2.30.0` и `pydantic==2.12.5`
    - Не трогать `deepeval==3.9.3`, `deepteam==1.0.6`, `openai==2.30.0` — deepeval должен продолжить работать (DEP-02)

    Если в конце файла нет newline — добавить его перед новыми строками.
  </action>
  <acceptance_criteria>
    - `grep -c "^ragas>=0.2.0$" requirements.txt` = 1
    - `grep -c "^langchain-openai>=0.1.0$" requirements.txt` = 1
    - `grep -c "^langchain-core>=0.2.0$" requirements.txt` = 1
    - `grep -c "^deepeval==3.9.3$" requirements.txt` = 1 (не сломан)
    - `grep -c "^deepteam==1.0.6$" requirements.txt` = 1
    - `grep -cE "^langchain(==|>=)" requirements.txt` = 0 (монолитный langchain НЕ добавлен)
    - Общее число строк в файле = 20 (было 17 + 3 новых)
  </acceptance_criteria>
  <verify>
    <automated>python -m pip install --dry-run -r requirements.txt 2>&amp;1 | tail -5 &amp;&amp; grep -E "^(ragas|langchain-openai|langchain-core|deepeval)" requirements.txt</automated>
  </verify>
  <done>
    Три новые строки добавлены в конец requirements.txt. Существующие зависимости нетронуты. `pip install --dry-run` не сообщает о конфликтах.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip → PyPI | Установка пакетов с внешнего индекса — риск supply chain |
| requirements.txt → dev machine | Файл определяет что будет установлено в рабочее окружение |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt | mitigate | Явные version constraints (>=X.Y.0) и фиксированные существующие версии (==); git diff будет виден при review |
| T-01-02 | Supply chain (Tampering) | PyPI packages ragas/langchain-* | accept | Пакеты публикуются известными организациями (Explodinggradients, LangChain Inc); risk acceptance — проект уже доверяет openai, anthropic с PyPI |
| T-01-03 | DoS (Denial of Service) | dependency resolution | mitigate | Использование `>=` вместо `==` для новых пакетов позволяет resolver подобрать совместимые версии и избежать тупика с openai==2.30.0/pydantic==2.12.5 |
</threat_model>

<verification>
- pip dry-run проходит без ERROR/conflict
- `diff` requirements.txt показывает только 3 добавленные строки в конце
- deepeval==3.9.3 строка не изменена
</verification>

<success_criteria>
requirements.txt содержит три новых строки (ragas, langchain-openai, langchain-core) без поломки существующих закреплённых версий. `pip install --dry-run -r requirements.txt` завершается успешно.
</success_criteria>

<output>
После завершения создать `.planning/phases/01-python-pipeline/01-01-SUMMARY.md`
</output>
