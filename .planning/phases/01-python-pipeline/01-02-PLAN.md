---
phase: 01-python-pipeline
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - eval/custom_metrics/__init__.py
  - eval/custom_metrics/example_metric.py
autonomous: true
requirements:
  - RAGAS-07
must_haves:
  truths:
    - "Папка eval/custom_metrics/ существует как Python-пакет (__init__.py)"
    - "Файл eval/custom_metrics/example_metric.py содержит класс, наследующий MetricWithLLM + SingleTurnMetric"
    - "Класс имеет атрибут name и async метод _single_turn_ascore"
    - "Пайплайн (plan 03) сможет импортировать подкласс MetricWithLLM+SingleTurnMetric из eval/custom_metrics/*.py через автодискавери"
  artifacts:
    - path: "eval/custom_metrics/__init__.py"
      provides: "Маркер Python-пакета для autodiscovery"
      min_lines: 1
    - path: "eval/custom_metrics/example_metric.py"
      provides: "Пример кастомной метрики RAGAS с документацией на русском"
      contains: "class ExampleCustomMetric"
  key_links:
    - from: "eval/custom_metrics/example_metric.py"
      to: "ragas.metrics.base.MetricWithLLM"
      via: "class ExampleCustomMetric(MetricWithLLM, SingleTurnMetric)"
      pattern: "MetricWithLLM.*SingleTurnMetric"
    - from: "eval/custom_metrics/example_metric.py"
      to: "eval/eval_ragas_metrics.py (plan 03)"
      via: "автодискавери через importlib + pkgutil"
      pattern: "issubclass.*MetricWithLLM"
---

<objective>
Создать scaffold для кастомных RAGAS-метрик: Python-пакет `eval/custom_metrics/` с рабочим примером (`example_metric.py`), от которого plan 03 сможет отталкиваться при автодискавери. Пример должен компилироваться и иметь всю необходимую сигнатуру MetricWithLLM + SingleTurnMetric, чтобы `issubclass()`-чек в пайплайне подхватил его.

Purpose: RAGAS-07 требует автодискавери кастомных метрик. Без этого скаффолда plan 03 не сможет протестировать автодискавери, а пользователь не получит копируемый шаблон. Файлы независимы от requirements.txt и не конфликтуют с пайплайном — можно делать параллельно plan 03 с точки зрения записи, но логически plan 03 импортирует этот пакет, поэтому ставим в Wave 2 (после установки зависимостей).
Output: Два файла — пустой __init__.py и example_metric.py с классом ExampleCustomMetric, который импортируется без ошибок после установки requirements.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-python-pipeline/01-CONTEXT.md
@.planning/phases/01-python-pipeline/01-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/STACK.md

<interfaces>
<!-- Ключевые контракты RAGAS v0.2+ — executor использует эти сигнатуры напрямую -->

From ragas.metrics.base (ragas>=0.2):
```python
class MetricWithLLM:
    # базовый класс для метрик, использующих LLM-судью
    llm: LangchainLLMWrapper  # инжектится при evaluate()

class SingleTurnMetric:
    # миксин — метрика работает с одним SingleTurnSample за раз
    async def _single_turn_ascore(
        self, sample: SingleTurnSample, callbacks=None
    ) -> float:
        ...  # вернуть float в диапазоне 0.0-1.0
```

From ragas.dataset_schema:
```python
class SingleTurnSample:
    user_input: str
    response: str
    retrieved_contexts: list[str] | None
    reference: str | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create custom_metrics package marker</name>
  <read_first>
    - eval/scripts/__init__.py (пустой файл — паттерн из 01-PATTERNS.md, раздел custom_metrics/__init__.py)
    - .planning/phases/01-python-pipeline/01-PATTERNS.md (раздел `eval/custom_metrics/__init__.py`)
  </read_first>
  <files>eval/custom_metrics/__init__.py</files>
  <action>
    Создать файл `eval/custom_metrics/__init__.py` с коротким комментарием-маркером на русском (аналогично пустому паттерну из `eval/scripts/__init__.py`, но с пояснением для пользователя, который будет копировать example_metric.py).

    Содержимое файла — ровно эти 3 строки:

    ```python
    # Пакет для кастомных метрик RAGAS.
    # Любой *.py файл внутри, содержащий подкласс MetricWithLLM + SingleTurnMetric,
    # будет автоматически подхвачен пайплайном eval/eval_ragas_metrics.py (RAGAS-07).
    ```

    Правила:
    - НЕ экспортировать ничего через `__all__` — автодискавери в plan 03 будет сканировать файлы через `pkgutil.iter_modules`, не через `__all__`
    - НЕ импортировать ragas здесь — иначе падение при отсутствии ragas ударит по всем импортам пакета
    - Файл должен быть валидным Python (3 строки комментария — валидный модуль)
  </action>
  <acceptance_criteria>
    - Файл `eval/custom_metrics/__init__.py` существует
    - `python -c "import eval.custom_metrics"` завершается exit 0 (не падает)
    - `grep -c "MetricWithLLM" eval/custom_metrics/__init__.py` = 1 (упоминание в комментарии есть)
    - Число строк в файле ≥ 3 и ≤ 5
    - В файле нет `import ragas` и нет `import langchain`
  </acceptance_criteria>
  <verify>
    <automated>test -f eval/custom_metrics/__init__.py && python -c "import importlib.util; s = importlib.util.spec_from_file_location('m','eval/custom_metrics/__init__.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); print('OK')"</automated>
  </verify>
  <done>
    Пакет-маркер создан, Python может импортировать модуль без ошибок, нет преждевременных импортов ragas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ExampleCustomMetric template</name>
  <read_first>
    - .planning/phases/01-python-pipeline/01-PATTERNS.md (раздел `eval/custom_metrics/example_metric.py` — шаблон)
    - .planning/research/FEATURES.md (раздел "Кастомные метрики" — API и минимальный шаблон с промптом)
    - .planning/phases/01-python-pipeline/01-CONTEXT.md (D-08 про автодискавери MetricWithLLM+SingleTurnMetric)
  </read_first>
  <files>eval/custom_metrics/example_metric.py</files>
  <action>
    Создать файл `eval/custom_metrics/example_metric.py` — рабочий пример кастомной RAGAS-метрики, который:
    1) Импортируется без ошибок после установки ragas>=0.2 (plan 01)
    2) Является подклассом `MetricWithLLM` + `SingleTurnMetric` — plan 03 должен его автоматически найти через `issubclass(cls, MetricWithLLM) and issubclass(cls, SingleTurnMetric)`
    3) Имеет рабочую (не NotImplementedError) реализацию `_single_turn_ascore`, возвращающую float 0.0-1.0 — иначе прогон будет падать при autodiscovery включённом

    ТОЧНЫЕ импорты (per STACK.md и FEATURES.md):
    ```python
    from dataclasses import dataclass, field
    from typing import Optional
    from ragas.metrics.base import MetricWithLLM, SingleTurnMetric
    from ragas.dataset_schema import SingleTurnSample
    from ragas.prompt import PydanticPrompt
    from pydantic import BaseModel
    ```

    Структура файла (все блоки обязательны):

    1. **Модульный docstring на русском** (минимум 15 строк): что делает метрика, как создавать свою, где разместить файл (`eval/custom_metrics/my_metric.py`), как подключается автодискавери. Упомянуть что имя класса не важно — важен базовый класс.

    2. **Класс вывода LLM** (Pydantic):
    ```python
    class CompletenessOutput(BaseModel):
        """Структура ответа LLM-судьи: оценка полноты + объяснение."""
        score: float  # 0.0-1.0
        reason: str   # объяснение на русском
    ```

    3. **PydanticPrompt** с инструкцией на русском:
    ```python
    class CompletenessPrompt(PydanticPrompt[dict, CompletenessOutput]):
        instruction = (
            "Ты оцениваешь полноту ответа ассистента относительно вопроса пользователя. "
            "Верни score от 0.0 (ответ неполный/пустой) до 1.0 (ответ полностью покрывает вопрос). "
            "В поле reason напиши объяснение на русском языке."
        )
        input_model = dict
        output_model = CompletenessOutput
    ```

    4. **Метрика**:
    ```python
    @dataclass
    class ExampleCustomMetric(MetricWithLLM, SingleTurnMetric):
        """Пример кастомной метрики: Completeness (полнота ответа).

        Диапазон: 0.0-1.0. Требует только user_input и response
        (retrieved_contexts и reference не обязательны).

        Подмени name и логику _single_turn_ascore под свою задачу,
        не меняя базовые классы — иначе автодискавери в
        eval_ragas_metrics.py тебя не найдёт.
        """
        name: str = "completeness"
        completeness_prompt: CompletenessPrompt = field(default_factory=CompletenessPrompt)

        async def _single_turn_ascore(
            self, sample: SingleTurnSample, callbacks=None
        ) -> float:
            out = await self.completeness_prompt.generate(
                data={"question": sample.user_input, "answer": sample.response},
                llm=self.llm,
            )
            # Защита от LLM-галлюцинаций: clamp в диапазон 0.0-1.0
            return max(0.0, min(1.0, float(out.score)))
    ```

    Правила:
    - НЕ использовать `raise NotImplementedError` — пайплайн должен уметь его реально вычислить
    - Docstrings — на русском (per RAGAS-07 и CUST-01, хоть CUST-01 формально в фазе 3, здесь уже на русском — иначе пользователь будет копировать английский boilerplate)
    - НЕ делать отдельных inline-импортов внутри методов — все import-ы наверху файла
    - НЕ экспортировать класс через `from eval.custom_metrics import ExampleCustomMetric` — автодискавери работает через pkgutil
  </action>
  <acceptance_criteria>
    - Файл `eval/custom_metrics/example_metric.py` существует
    - `grep -c "class ExampleCustomMetric(MetricWithLLM, SingleTurnMetric)" eval/custom_metrics/example_metric.py` = 1
    - `grep -c "class CompletenessOutput(BaseModel)" eval/custom_metrics/example_metric.py` = 1
    - `grep -c "class CompletenessPrompt(PydanticPrompt" eval/custom_metrics/example_metric.py` = 1
    - `grep -c "async def _single_turn_ascore" eval/custom_metrics/example_metric.py` = 1
    - `grep -c "NotImplementedError" eval/custom_metrics/example_metric.py` = 0
    - `grep -c "name: str = " eval/custom_metrics/example_metric.py` ≥ 1
    - `grep -cE "^from ragas" eval/custom_metrics/example_metric.py` ≥ 2
    - Количество строк в файле ≥ 40 и ≤ 120 (шаблон должен быть читаемым, но не перегружённым)
    - `python -c "import ast; ast.parse(open('eval/custom_metrics/example_metric.py').read())"` завершается exit 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; ast.parse(open('eval/custom_metrics/example_metric.py').read()); print('AST OK')" &amp;&amp; grep -E "class ExampleCustomMetric\(MetricWithLLM, SingleTurnMetric\)" eval/custom_metrics/example_metric.py</automated>
  </verify>
  <done>
    Файл компилируется (AST валиден), содержит подкласс MetricWithLLM+SingleTurnMetric с именем ExampleCustomMetric, рабочую реализацию _single_turn_ascore (без NotImplementedError), и docstring на русском. После установки ragas>=0.2 класс будет импортируемым.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/custom_metrics/*.py → Python interpreter | Пользователь копирует example_metric.py и пишет свой код — плагин-подобная точка расширения |
| LLM response → CompletenessOutput parsing | LLM-судья может вернуть некорректный score (>1, <0, NaN, строка) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | eval/custom_metrics/*.py (user code) | accept | Пользователь — сам разработчик проекта (solo dev); код не исполняется от внешних пользователей. Автодискавери в plan 03 сканирует только файлы в eval/custom_metrics/, не произвольные пути |
| T-01-02-02 | Tampering (LLM output) | CompletenessOutput.score | mitigate | `max(0.0, min(1.0, float(out.score)))` clamp в _single_turn_ascore — защита от score вне диапазона. PydanticPrompt валидирует типы через Pydantic — нестроковое/нечисловое значение упадёт с ValidationError, не проскочит молча |
| T-01-02-03 | Information Disclosure | docstring / prompt instruction | accept | Инструкция промпта на русском — не содержит секретов или API-ключей. `self.llm` инжектится пайплайном из .env, не из этого файла |
| T-01-02-04 | DoS | _single_turn_ascore | mitigate | RunConfig(max_wait=120) в plan 03 ограничит время одного вызова. Пользователь должен знать (docstring упоминает `diapazon 0.0-1.0`), что долгие вычисления внутри _ascore блокируют весь прогон |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('eval/custom_metrics/example_metric.py').read())"` — AST валиден
- `python -c "import eval.custom_metrics"` — пакет импортируется (пустой __init__.py не падает)
- После `pip install -r requirements.txt` (plan 01): `python -c "from eval.custom_metrics.example_metric import ExampleCustomMetric; from ragas.metrics.base import MetricWithLLM, SingleTurnMetric; assert issubclass(ExampleCustomMetric, MetricWithLLM); assert issubclass(ExampleCustomMetric, SingleTurnMetric); print('OK')"` — наследование корректно
- `grep -c "NotImplementedError" eval/custom_metrics/example_metric.py` = 0 — метрика реально вычислима
</verification>

<success_criteria>
Пакет `eval/custom_metrics/` существует как Python-модуль с рабочим примером `ExampleCustomMetric`. Класс наследует MetricWithLLM + SingleTurnMetric с именем "completeness" и реализованным _single_turn_ascore. Plan 03 сможет обнаружить этот класс через автодискавери (pkgutil + issubclass) и передать его в `evaluate()` как часть списка metrics.
</success_criteria>

<output>
После завершения создать `.planning/phases/01-python-pipeline/01-02-SUMMARY.md`
</output>
